import { Module } from '@nestjs/common';

// Wired up incrementally through Phase 2 (repository, service, controller,
// reaper) — see task 2.7 for the final provider/controller wiring.
@Module({})
export class InventoryModule {}
